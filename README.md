Well look what you've stumbled upon. 
This is a grAin created machine learning pipeline broken down into two main stages: data processing and model training.
We've got ourselves one Jupyter Notebook (process.ipynb) that we use for the data preparation (or preprocessing) phase.
But not so fast, for there is another. That one handles the actual machine learning model training. We called it train.ipynb, which is a great name for a notebook that does training. 
There's also a CLIP.png file that attempts to demostrate the contrastive pre-training process using dogs. Did it work? Only you can answer that. 

Look, this is basically a barebones, two-step machine learning workflow (Process -> Train) built using Jupyter Notebooks for processing image/text data and training a model associated with the CLIP architecture. If that's not what you wanted just leave. 
Don't forget to like and subscribe. 
